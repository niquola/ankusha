(ns ankusha.core
  (:require [cljs.core.async :as async :refer [go alt!]]
            [ankusha.consul :as consul]
            [ankusha.pg :as pg])
  (:gen-class))

(defn -main [])

(comment
  (-main)
  )
